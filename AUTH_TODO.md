# 인증 연동 진행 상황 및 다음 단계 (진척도 알림)

이 파일은 구글/본인 인증을 연동하려던 마지막 시점을 기억하기 위해 생성된 파일입니다.
다음 로그인/인증 연동 작업 시 이 노트를 참고하세요.

## 📌 현재 상태 (안티그래비티 마지막 메모)
- 사용자가 "구글 인증" 혹은 "본인 인증" 설정을 외부 콘솔(Firebase, GCP, 또는 통신사 인증 등)에서 완료한 상태.
- 프로젝트 내에는 아직 **Firebase SDK** 또는 **Google OAuth 라이브러리** 등 구글/본인 인증 관련 패키지가 `npm install` 되지 않았습니다.
- `src/components/screens/AuthScreens.tsx`의 "구글 이메일 로그인" 버튼은 아직 임시 프론트엔드 모의(Mock) 코드로 동작하고 있습니다. (`onLogin(provider + '@user.com')`)

## 🚀 다음 세션에서 진행할 핵심 목표
다음 기능을 이어서 구현할 때 다음 세 가지 작업을 차례로 실행합니다.

1. **상황 파악 및 세팅**
   - 구글 소셜 로그인(Firebase 연동)을 구축할지? 
   - 다날/PASS 등 한국형 실명 인증(PortOne 등) 모듈을 결합할지 명확히 결정하여 터미널에서 패키지를 설치합니다 (`npm install firebase` 등).

2. **환경 설정 (보안)**
   - 선택된 방식에 맞춰 `.env` 파일에 필요한 API 키 및 클라이언트 ID를 세팅합니다.
   - `src/lib/firebase.ts` (또는 auth 관련 파일) 생성 

3. **`AuthScreens.tsx` 프론트엔드 연동**
   - 기존의 하드코딩된 로그인 버튼을 `signInWithPopup(auth, provider)` 같은 실제 API 콜로 변경합니다.
   - 성공/실패 시의 UI 토스트 팝업이나 화면 전환 처리를 완성합니다.

---
> **사용자님께 (다음 대화를 위한 안내):**
> 다음번에 저(안티그래비티)와 새로운 대화를 여시거나 질문하실 때,
> **"저번 구글/본인 인증 연동 이어서 해 줘"** 또는 **"AUTH_TODO.md 참고해서 연동 시작하자"**라고만 말씀해 주시면, 제가 저장된 이 기록을 바탕으로 즉시 코드 설정을 시작하겠습니다! 오늘 수고하셨습니다.
